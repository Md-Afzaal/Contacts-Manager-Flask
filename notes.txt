This is a note for future me who is so pathetic that he has to come back and see how to the same thing again.
IDK how he is gonna do shit in the future. Anyway here it is the baby notes:

#to create env:
python -m venv <name of your env like: venv>

#to activate env:
source venv/bin/activate (macos)
venv/Scripts/activate (windows)

#to deactivate:
type: "deactivate" dumbass.

#Now to install dependencies:
tap into venv;
pip install <type dependencies you want like: Flask Flask-Scss Flask-SQLAlchemy>

[ Note: "pip list" to check for list of dependencies ]

#to add requirements.txt:
pip freeze > requirements.txt
[
    pip freeze: lists all the dependencies
    > : redirects that output into a file
    creates/overwrites requirements.txt with the list
]

# create a templates folder to store html files

